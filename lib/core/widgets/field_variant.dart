/// Rendering style shared by [DateFieldWidget] and [TimeFieldWidget].
///
/// [field] is the labelled `InputDecorator` used inside forms and bottom
/// sheets. [pill] is the compact tappable chip used where the value sits
/// inline in a header and has no room for a label.
enum FieldVariant { field, pill }
